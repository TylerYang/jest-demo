import React from 'react';

class Button extends React.component {
    render() {
        return (
            <Button>
                whatever
            </Button>
        );
    }
}

export default Button;
